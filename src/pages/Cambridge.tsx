import { Header } from "@/components/Header";
import { HeroSectionCambridge } from "@/components/HeroSectionCambridge";

const Cambridge = () => {
  return (
    <div className="min-h-screen bg-background">
      <main>
        <HeroSectionCambridge />
      </main>
    </div>
  );
};

export default Cambridge;